'use client'

export default function page(){

    return (
        <>
        <h1>hydration problem</h1>
        <button type="button" onClick={() => alert('Button clicked!')}>
                Click Me
            </button>

        </>
    

);
}